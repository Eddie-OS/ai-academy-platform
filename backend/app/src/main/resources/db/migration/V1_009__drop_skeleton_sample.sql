-- =============================================================================
-- V1_009__drop_skeleton_sample.sql
--
-- 删除阶段 0 的骨架示例表 sys_skeleton_sample（V0_001 建）。
--
-- 为什么现在删、而不是改 V0_001：规则 DB-3——已合并的脚本禁止修改，修正一律新增脚本。
-- 直接改 V0_001 会让已经跑过迁移的库校验和不匹配，Flyway 启动即失败。
--
-- 为什么删得这么晚：它曾是全项目唯一的写接口，E0 冒烟测试的 7 项断言（未登录 401 /
-- 查看账号写 403 / 统一响应格式 / traceId 透传 / 参数校验）都挂在它身上。1C 的导入中心
-- 带来了第一批真实写接口，冒烟测试已改指到 /api/imports，本表和它的四层代码同时清掉，
-- 不留过渡代码。
--
-- pg_trgm 扩展是 V0_001 里另一件事，保留：阶段 2 的名称模糊搜索要用（16.1.5）。
-- =============================================================================

DROP TABLE IF EXISTS sys_skeleton_sample;
