-- =============================================================================
-- V2_002__demand_course_link_note.sql
--
-- 阶段 2 B-4：需求↔课程关联落地时补「关联说明」列。
--
-- 需求 8.4 的字段表有四项：需求ID、课程ID、关联说明（≤200 字）、创建人/创建时间，而
-- 开发实施文档 6.3.1 描述这张表时只强调了 N:N 与双向索引，没列字段清单，阶段 1 建表时
-- 因此漏掉了关联说明。按 DB-3「已合并的脚本禁止修改」，用新脚本补而不是改 V1_003。
--
-- 这一列的用处是说清「这门课解决了该需求的哪一部分」：一个需求常常拆成几门课，只看课程名
-- 分不出各自覆盖的范围。
-- =============================================================================

ALTER TABLE rel_demand_course
    ADD COLUMN link_note VARCHAR(200);

COMMENT ON COLUMN rel_demand_course.link_note IS '关联说明（需求 8.4）。说明该课程解决该需求的哪一部分，选填';
