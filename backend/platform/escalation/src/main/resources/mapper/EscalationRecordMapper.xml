<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiacademy.platform.escalation.repository.EscalationRecordMapper">

    <sql id="filter">
        deleted = FALSE
        <if test="q.objectType != null and q.objectType != ''">
            AND object_type = #{q.objectType}
        </if>
        <if test="q.objectId != null">
            AND object_id = #{q.objectId}
        </if>
        <if test="q.escalateType != null and q.escalateType != ''">
            AND escalate_type = #{q.escalateType}
        </if>
        <if test="q.ownerNo != null and q.ownerNo != ''">
            AND owner_no = #{q.ownerNo}
        </if>
        <if test="q.source != null and q.source != ''">
            AND source = #{q.source}
        </if>
        <if test="q.escalatedFrom != null">
            AND escalated_at &gt;= #{q.escalatedFrom}
        </if>
        <if test="q.escalatedTo != null">
            AND escalated_at &lt;= #{q.escalatedTo}
        </if>
        <if test="q.keyword != null and q.keyword != ''">
            AND (object_name ILIKE CONCAT('%', #{q.keyword}, '%')
                 OR content ILIKE CONCAT('%', #{q.keyword}, '%')
                 OR owner_name ILIKE CONCAT('%', #{q.keyword}, '%'))
        </if>
    </sql>

    <select id="countByQuery" resultType="long">
        SELECT COUNT(*) FROM dtl_escalation_record WHERE
        <include refid="filter"/>
    </select>

    <select id="pageByQuery"
            resultType="com.aiacademy.platform.escalation.domain.EscalationRecord">
        SELECT id, object_type AS objectType, object_id AS objectId, object_name AS objectName,
               owner_no AS ownerNo, owner_name AS ownerName, escalate_type AS escalateType,
               channel_note AS channelNote, remark, escalated_at AS escalatedAt,
               process_node AS processNode, light, source, content,
               created_at AS createdAt, created_by AS createdBy
          FROM dtl_escalation_record
         WHERE
        <include refid="filter"/>
         ORDER BY escalated_at DESC
         LIMIT #{q.pageSize} OFFSET #{q.offset}
    </select>
</mapper>
