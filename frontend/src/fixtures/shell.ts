/**
 * 壳层的冻结角标。
 *
 * <p>《设计文档 V2.0》第 4 章的九张截图里，顶栏「任务 12」「消息 8」与侧栏「任务中心 12」
 * 「消息中心 8」是同一对数，九页一致。阶段 3 的 {@code aggregate/worklist} 与阶段 4 的
 * {@code platform/escalation} 接上后这两个常量整批消失。
 *
 * <h3>为什么单独一个文件而不是写在壳层里</h3>
 *
 * 待办总数在总看板上出现三次：顶栏角标、侧栏角标，以及 R6「待办行动清单」标题旁的计数。
 * 常量写在壳层内部时，R6 拿不到它，只能退而用<b>表格行数</b>（冻结数据是 5 行），
 * 于是同一屏上「待办行动清单 5」与「共 12 项待办需要跟进」并存 ——
 * 两个数各自都合理，唯一能发现的办法是有人把它们放在一起读。
 */
export const PENDING_TASK_TOTAL = 12;

/** 催办台账的待处理条数（界面名「消息中心」，见 V-1） */
export const PENDING_ESCALATION_TOTAL = 8;
