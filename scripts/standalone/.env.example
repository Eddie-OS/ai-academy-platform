# 单机模式的环境变量模板。
#
# 用法：copy .env.example .env，然后用 .\hash.ps1 生成两个哈希填到下面。
# .env 已在 .gitignore 里，不会进版本库——口令哈希不进仓库、不进交付包，只在本机存在。
#
# 注意：这里没有 DB_HOST / DB_USER / DB_PASSWORD。
# 单机模式的数据库由 app.jar 自己拉起（EmbeddedPostgresBootstrap），
# 只监听 localhost、由本进程独占、trust 认证，没有连接口令这回事。

# 运营账号（可写全部业务数据）
OPERATOR_PASSWORD_HASH=

# 用户账号（只读，外加点赞与评论两个接口）
VIEWER_PASSWORD_HASH=

# 值的形状：{bcrypt}$2a$10$ 之后跟 53 个字符（22 位盐 + 31 位摘要），整串约 
# 60 个字符。这里刻意不给一个可直接复制的样例——公开可查的哈希被人取消注释直接用上，
# 等于系统口令是公开的，而它看起来和一个正常配置完全一样。
#
# 值里含 $ 符号，直接粘贴即可，不要加引号、不要转义。
# start.ps1 读这个文件时不做变量插值（docker compose 会，那会把 $2a 一类的片段吃掉，
# 产出一个能通过前缀检查但登录永远失败的残缺哈希）。
