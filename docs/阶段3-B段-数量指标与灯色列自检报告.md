# 阶段 3 · B 段（数量类指标 + 驾驶舱卡接线 + 列表灯色列）自检报告

> 对象：阶段 3B 计划出口——15.1 共 28 项数量 SQL、五驾驶舱 MetricCard 填数、四类列表灯色列与培训／案例 light 筛选。  
> 结论：见文末「出口核对」；效率／互动／讲师授课卡与总看板仍留给 3C／3D。  
> 自检日期：2026-08-03。

## 一、本段交付

| 项 | 内容 |
|---|---|
| metrics | `QuantityMetricsMapper`／`QuantityMetricsService`（`all()` + 五 cockpit VO）；状态从 `*StateMachines` 推导后 `@Param` 传入（E2-6） |
| API | `GET /api/metrics/quantity/{demands\|courses\|lecturers\|trainings\|cases}` → `MetricsController` |
| 前端 | `shared/api/metrics.ts`；`mergeMetricValues`；五 legacy `*CockpitPage` 接线；未接入 key 仍「阶段 3 接入」 |
| 灯色列 | VO：`light`／`lightDays`／`lightReason`；`WarningLightAssembler` + `calcMany`；培训计划／案例 Mapper `calc_light` 筛选；Meta `灯色` 四码 |
| 前端灯 | `WarningLightCell`；Demand／Course／TrainingPlan／Case 列表列；灯色筛选下拉 |
| 测试 | `QuantityMetricsIntegrationTest`（28 项造数 + cockpit keys + 红灯 VO） |
| 文档 | 本报告；[`指标口径核对表.md`](./指标口径核对表.md) 15.1 骨架 |

### 驾驶舱卡填数策略（一次定死）

| 驾驶舱 | 填真数的 key | 仍 pending |
|---|---|---|
| 需求 | total／pending／developing | cycle（15.2） |
| 课程 | total／developing／published／quality | cycle（15.2） |
| 讲师 | pool／qualified | attendees／active（15.3） |
| 培训 | plans／weekPlans／sessions／attendees／attendance／archive | — |
| 案例 | total／published | views／likes／comments（15.5） |

## 二、出口核对

| # | 出口 | 证据 | 状态 |
|---|---|---|---|
| 1 | 28 数量 SQL + 集成测 | `QuantityMetricsIntegrationTest.二十八项构造数据` | **通过** |
| 2 | 五驾驶舱 15.1 相关卡真数 | `forDemands` 等 + 前端 `mergeMetricValues` | 已接线 |
| 3 | 四类列表灯色 + 培训／案例可筛 | VO 装配；Plan／Case Mapper；`WarningLightCell` | 已落地 |
| 4 | 核对表 15.1 骨架 | `docs/指标口径核对表.md` | 已落 |
| 5 | 全量后端回归 | `gradlew test` 全绿（约 2026-08-03） | **通过** |

## 三、纪律对照

- 无缓存／无物化视图（U2）
- 不做需求转课程比例（U6）、不做覆盖率／部门维度
- 灯色实时算；快照仍只给变化检测
- Demand 退出预警口径与列表筛选对齐：`delivery_mark`（`WarningObjectKind.DEMAND`）

## 四、未做（留给 3C／3D）

- 15.2 效率指标与卡上 `cycle`
- 15.3 讲师授课／活跃；15.5 案例互动三卡
- `GET /api/dashboard/overview`、V2 去 fixture
