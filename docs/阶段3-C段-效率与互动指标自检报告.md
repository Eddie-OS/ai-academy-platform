# 阶段 3 · C 段（效率／讲师授课／案例互动 + 驾驶舱卡收尾）自检报告

> 对象：阶段 3C 计划出口——15.2 共 9 项效率 SQL（E1=MIN）、15.3／15.5 可落地项、五驾驶舱顶部卡无「阶段 3 接入」（本屏范围）。  
> 结论：见文末「出口核对」。总看板／V2 去 fixture／任务中心完整前端留给 3D。  
> 自检日期：2026-08-04。

## 一、本段交付

| 项 | 内容 |
|---|---|
| 效率 | `EfficiencyMetricsMapper`／`Service`／`EfficiencySnapshot`；`GET /api/metrics/efficiency/summary` |
| 数量扩 | `forLecturers`→`attendees`／`active`；`forCases`→`views`／`likes`／`comments` |
| 15.3／15.5 其余 | `LecturerMetricsService`／`InteractionMetricsService` 的 `all()` + 按参方法 |
| 前端 | `efficiencySummary`；`mergeCycleMetric`（1 位小数 + suffix「天」）；Demand／Course 合并 cycle；讲师／案例数量 key 自动填卡 |
| 测试 | `EfficiencyMetricsIntegrationTest`（回退 MIN）；`LecturerInteractionMetricsIntegrationTest`；扩 `QuantityMetricsIntegrationTest` |
| 文档 | 本报告；[`指标口径核对表.md`](./指标口径核对表.md) 补 15.2／15.3／15.5 |

### 驾驶舱卡（本段后应全部有真数或「—」）

| 驾驶舱 | key | 口径 |
|---|---|---|
| 需求 | cycle | 15.2 #1，区间默认全部；无样本 → null →「—」 |
| 课程 | cycle | 15.2 #3 |
| 讲师 | attendees／active | 15.3 #5 COUNT 本月结束态场次；#7 近 90 天 DISTINCT |
| 案例 | views／likes／comments | 15.5 #1–3 全库 COUNT |

## 二、出口核对

| # | 出口 | 证据 | 状态 |
|---|---|---|---|
| 1 | 15.2 九项 + 回退测 | `EfficiencyMetricsIntegrationTest`（回退 MIN + #2／#4～#9 增量） | **通过** |
| 2 | 驾驶舱 cycle／讲师／案例卡 | summary API + quantity 扩 key + 前端 merge | 已接线 |
| 3 | 核对表 15.2／15.3／15.5 | `docs/指标口径核对表.md` | 已落 |
| 4 | 全量后端回归 | `gradlew test`（2026-08-04 BUILD SUCCESSFUL） | **通过** |

## 三、纪律对照

- E1 取首次 MIN；测试含「发布→优化→再发布」
- 无预聚合／缓存；不做 U6 转化率、覆盖率（15.4）
- M-1：不读不写 `dtl_teaching_record`
- R10：正式评分与试讲反馈分表；单测正式 5 + 试讲 1 → 均分 5.0
- E2-6：效率 SQL 状态由 `*StateMachines` 参数传入

## 四、未做（留给 3D）

- `GET /api/dashboard/overview`、业务价值填报
- V2 去 fixture、任务中心完整前端
- 预警明细页（若未做）
