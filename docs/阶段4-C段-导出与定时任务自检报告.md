# 阶段 4C　导出与定时任务自检报告

> 自检日期：2026-08-04。依据：开发 5.11.2；阶段 4 提示词实现清单 6、8～10。

## 交付物

| 项 | 路径／证据 |
|---|---|
| 导出任务表 | `V4_003__sys_export_task.sql` |
| 任务运行日志 | `V4_004__sys_job_run_log.sql` |
| 同步／异步导出 | `ListExportService`；≤2000 同步流；>2000 异步 + `GET /api/exports/{id}` |
| 列表导出入口 | `DemandController`／`CourseController`／`CaseController` 的 `GET .../export`（Query 与列表同型） |
| 前端轮询 | `shared/api/exports.ts`（3s） |
| 导出清理 | `ExportCleanupJob`（03:30，7 天 + 5GB） |
| 数据库备份 | `DatabaseBackupJob`（02:00，`pg_dump`，PowerShell／Windows 路径） |
| 灯色／附件写日志 | `WarningLightSnapshotJob`、`AttachmentCleanupJobBridge` → `JobRunLogger` |
| 恢复步骤 | [`备份恢复验证.md`](./备份恢复验证.md) |

## 硬约束核对

- EasyExcel 流式写；无水印；无 WebSocket／SSE
- 不引入 Redis／ShedLock
- `@Scheduled` 注释标明单实例假设
- Mapper 位于 `com.aiacademy.**.repository`（符合 `@MapperScan`）

## 自动化／回归

| 项 | 结果 |
|---|---|
| 既有集成测试（含催办／评审） | 通过 |
| 空库 restore 实测 | **待人工**（见备份恢复验证文档步骤） |
