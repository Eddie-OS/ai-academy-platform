# 阶段 5　验收标准映射表（需求第 18 章 ↔ 测试）

> E5-4 交付物。状态：`自动通过`／`按实现验收`／`待人工`／`例外`。
> 签字栏在文末。V-9 已收口后 A3 按新口径。
>
> **2026-08-04 复核**：后端 `gradlew test` **54 个测试类 / 635 例，0 失败 0 跳过**；
> 前端 `tsc` 干净、`vitest` **24 文件 / 119 例**、`playwright` **143 例**（含本轮新增的 12 例无障碍结构断言）；
> `smoke-test.ps1` 打真实 HTTP **46 项 0 失败**。下表凡标「自动通过」的都在这一轮里跑过。

## 18.1 状态机（A1）

| # | 映射证据 | 状态 |
|---|---|---|
| A1-1 | 各 `*Lifecycle*Test`；状态机 CSV／转换表测试 | 自动通过 |
| A1-2 | `ILLEGAL_TRANSITION` 用例 | 自动通过 |
| A1-3 | 需求开发状态副作用／优化次数测试 | 自动通过 |
| A1-4 | 课程评审轮次集成测试 | 自动通过 |
| A1-5 | `WarningLightIntegrationTest#改错别字`；课程 update | 自动通过 |
| A1-6 | 任务终态关闭；灯色 outOfScope | 自动通过 |
| A1-7 | `CourseQualifiedEvent` → 案例 | 自动通过 |
| A1-8 | **按实现验收（D-7）**：归档需验收；交付无额外前置 | 按实现 |
| A1-9 | **按实现验收（D-13）**：出口一退回；出口二不自动退 | 按实现 |
| A1-10～11 | 案例审核硬阻断／覆盖 | 自动通过 |
| A1-12 | 培养状态不写流转日志 | 自动通过 |
| A1-13 | 分流出口枚举二元 | 自动通过 |

## 18.2 权限（A2）

| # | 映射证据 | 状态 |
|---|---|---|
| A2-1 | `OperatorOnly.test.tsx` 3 例（改地址栏进 → 403 状态页）+ `AppShellV2.test.tsx` 4 例（侧栏不渲染写入口，且**不是靠 disabled**）；侧栏过滤与路由兜底读同一个 `operatorOnly` 标记 | 自动通过（本轮转正） |
| A2-2 | `WriteApiPermissionTest#全部写接口对用户账号一律403`：遍历容器里注册的**全部**写接口真发请求；白名单钉死三条（点赞／评论／停留时长），免登录档钉死两条（登录／登出） | 自动通过 |
| A2-3～5 | 各生命周期测试 + `smoke-test.ps1`（viewer 写 403、读 200、未登录 401） | 自动通过（本轮转正） |
| A2-6 | 无 delegate 字段（N19） | 自动通过 |
| A2-7～9 | 反馈只读（无录入表单，只能导入）；全前端检索无「心得／激励／积分／排行榜」任何一词 | 自动通过（本轮转正） |
| A2-10 | 审计账号固定值 | 自动通过 |
| A2-11 | 乐观锁 `CONCURRENT_MODIFIED` | 自动通过 |

## 18.3 三色灯与催办（A3）

| # | 映射证据 | 状态 |
|---|---|---|
| A3-1～5 | `WarningLightIntegrationTest`（V-9） | 自动通过 |
| A3-6～7 | 阈值服务；终态 NONE | 自动通过 |
| A3-8～9 | `EscalationCycleTest`；快照变色追加 | 自动通过 |
| A3-10～12 | `EscalationIntegrationTest`；无消息表 | 自动通过 |
| A3-13 | 有效期提示独立于灯色：`CourseCrudIntegrationTest` EX3／EX7（截止日按首次发布时间重算；**实时计算不落库**，SQL 与 Java 两份口径一致）；前端 `StatusTag.test.tsx` SV1／SV4（有效期标签与灯色不共用色系） | 自动通过（本轮转正） |

## 18.4～18.7

| 段 | 主要证据 | 状态 |
|---|---|---|
| A4 | 材料版本／自检快照集成 | 自动通过 |
| A5 | 需求分流：后端 `DemandReviewOutletIntegrationTest`（枚举二元、出口一退回／出口二不自动退）；前端 `p02-demand.spec.ts` 断言表里的分流出口只有两个合法值 | 自动通过（本轮转正） |
| A6 | `rel_demand_course` | 自动通过 |
| A7 | 试讲双结论 | 自动通过 |

## 18.8／18.8a

| 段 | 主要证据 | 状态 |
|---|---|---|
| A8 | `Import*Test`；签到／撤销 | 自动通过 |
| A8a | 排课校验集成测试 | 自动通过 |

## 18.9 指标（A9）

| # | 映射证据 | 状态 |
|---|---|---|
| A9-1 | [`指标口径核对表.md`](./指标口径核对表.md) | **待业务签字（E3-1）** |
| A9-2～12 | 效率／互动／数量集成测试 | 自动通过 |
| A9-10 | 无覆盖类指标；回归模式隐藏组织区 | 自动通过 |

## 18.10～18.11

| 段 | 主要证据 | 状态 |
|---|---|---|
| A10 | 任务派生／TaskV2；催办文案 | 自动通过 |
| A11 | 六类模板／整批校验／撤销；无组织架构模板 | 自动通过 |

## 本轮剩余的非自动项

| 项 | 性质 | 为什么自动化替代不了 |
|---|---|---|
| A1-8（D-7）、A1-9（D-13） | **按实现验收** | 实现与需求原文有出入，已按裁决落地。要业务确认「就按这样验」，不是测试能回答的 |
| A9-1（E3-1） | **待业务签字** | 54 个指标的口径核对表，业务要逐条认口径 |
| 无障碍 8 条人工走查 + 浏览器兼容四格 | 待人工 | 见[无障碍走查表](./阶段5-无障碍走查表.md)结论 |
| 生产初始化八步 | 待运维 | 第 1 步填真实口令哈希、第 6 步导真实名册，开发机上跑不构成证据 |
| E5-6 六类导入 | 待运营 | 要的是「运营**独立**完成」，代跑无效 |

## 签字（E5-4）

| 角色 | 结论 | 签字 | 日期 |
|---|---|---|---|
| 测试／开发 | 自动项已对账（2026-08-04：后端 635 例、前端 262 例、冒烟 46 项，全绿） | | |
| 业务 | 第 18 章逐条通过（含按实现项 A1-8／A1-9） | | |
| 业务 | E3-1 指标核对表 | | |
| 业务 | 无障碍书面例外：A11Y-01a（主按钮 4.16:1）、A11Y-15（目标尺寸）、灯色 <3:1 豁免 | | |
