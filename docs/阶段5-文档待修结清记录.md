# 阶段 5　文档待修清单结清记录

> 日期：2026-08-04。依据：阶段 5 §六「必须清空的欠账」第 2 项。  
> 原则：每条落到「改文档」或「按实现验收（修订单短注）」二选一，不允许继续挂账上线。

## 一、D 类（文档矛盾）

| # | 处置 | 说明 |
|---|---|---|
| D-1 | 改开发文档口径 | 表名以 6.2.7 `snapshot_warning_light` 为准；示例 DDL 视为过时 |
| D-2 | 记例外 | 三张无八种前缀表保留原名；`SchemaConventionTest` 已登记例外 |
| D-3 | 按实现 | 字典 3 类（作战单元／课程分类／自检）；激励与「反馈类型」不做 |
| D-4 | 按实现 | 业务验收表 `dtl_demand_acceptance`，归需求模块 |
| D-5／D-5a | 按实现 + 手册对齐 | 配置中心 Tab：阈值／字典／任务派生／自检／催办；自检为独立 Tab |
| D-6 | 已关闭 | 四个前端基础件 |
| D-7 | **按实现验收** | 交付标记无业务前置（C9）；A1-8 中「未验收不能交付」不测 |
| D-8 | 按实现 | `trigger_type` 已在 V2_001 |
| D-9 | 按实现 | `dtl_selfcheck_snapshot` 已建 |
| D-10 | 按实现 | 业务验收在需求驾驶舱，不在课程 A 段 |
| D-11 | 按实现 | `SET_ONLINE_DATES`／`INCREMENT_OPTIMIZE_COUNT` |
| D-12 | **按实现验收** | 重新评审只清出口，不清下游状态 |
| D-13 | **按实现验收** | 出口二验收不通过不自动退回，记 WARN；A1-9 出口二按此口径 |
| D-14 | 按实现 | `link_note` 已补 |
| D-15 | 按实现 | 归档表按 11.6 |
| D-16 | **按实现验收** | 归档完成标记不阻断状态转换（C2） |
| D-17 | 按实现 | 24 页=内容清单，一屏主从；见 `navigation.ts` |
| D-18 | 被 V-1 取代 | 界面名「消息中心」、语义催办台账 |
| D-19 | **改需求 6.2.5** | 用户可写白名单第三条：`PATCH /api/cases/{id}/views/{viewId}`（停留时长） |

## 二、S／M 类

| # | 处置 |
|---|---|
| S-1～S-3、S-5、S-6 | 推导字段以代码／Flyway 为准；运营手册不另发明字段 |
| S-4 | 已消解（D-15） |
| M-1 | 授课记录实时派生自场次+签到；表保留一期不写 |
| M-2 | `note_requirement` 三态 + `guide_text` |

## 三、P 类（纪律偏离）

| # | 处置 |
|---|---|
| P-1 | 见 [`openapi-gen` 脚本骨架](../frontend/scripts/generate-api-types.mjs) + 下文书面例外 |
| P-2 | 书面例外：模板无文件更新日期，不造假 |
| P-3 | 书面例外：5000 行由后端 I1 拦截 |
| P-4 | 已关闭（ECharts 可用后替换渲染） |
| P-5 | **关闭**：阶段 3 指标已接入 `cockpitMetrics`；驾驶舱卡读 overview／metrics 接口 |

## 四、P-1 书面例外（若离线无法装包）

- **已守住**：枚举／字典／导入类型字面量不手写，一律接口下发。
- **未完成**：`shared/api/*.ts` interface 仍手写对齐后端 record。
- **补救**：仓库已加 `frontend/scripts/generate-api-types.mjs`；网络可用时执行 `npm i -D openapi-typescript` 后 `node scripts/generate-api-types.mjs`。
- **不阻塞** E5-1～E5-6 其他项。

## 五、导入中心／配置中心设计走查

见 [`阶段5-导入与配置中心设计走查.md`](./阶段5-导入与配置中心设计走查.md)。
