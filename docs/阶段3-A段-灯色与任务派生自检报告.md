# 阶段 3 · A 段（指标地基 + 三色灯 + 任务派生）自检报告

> 对象：阶段 3A 计划出口——`Ratio`/`Average`、三色灯三边界 + L1、10 条 `DERIVE_TASK`、`CLOSE_RELATED_TASKS`、最小 `GET /api/tasks`、`DEFERRED` 清空任务两项。
> 结论：**本段出口全部通过**；54 指标／总看板／前端去 fixture 留给 3B～3D。
> 自检日期：2026-08-03。

## 一、本段交付

| 项 | 内容 |
|---|---|
| 基线提交 | `98ebe0f` 阶段 2 后端；`d14e303` 阶段 2 前端；`3f10cf2` 设计文档 V2.0 复刻（不含 cookies／playwright-report／test-results／_extracted） |
| metrics | `Ratio.of`／`Average.of`（U3／U4／U5）+ `RatioAverageTest` |
| warning | Flyway `V3_001` `calc_light`（STABLE，返回 BLUE/YELLOW/RED/NONE）；`WarningLightService`；需求／课程列表 `light` 筛选；每日 8:30 快照写 `snapshot_warning_light`（中文灯色映射，不发通知） |
| worklist | `DeriveTaskEffectHandler`／`CloseRelatedTasksEffectHandler` 移出 `DEFERRED`；`GET /api/tasks`（`ownerNo` + 逾期实时表达式） |
| 后端全量测试 | **`gradlew test` 全过**：app 360 + statemachine 256 + metrics 3，0 失败（报告在 `~/.ai-academy-build/**/reports/tests/test/`） |

## 二、出口准则证据

### 1. Ratio／Average 单测钉死边界

证据：`RatioAverageTest`

| 断言 | 规则 |
|---|---|
| `0/0`、`1/0` → `null` | U3 |
| `100%` → `100.0`；一位小数 HALF_UP | U5 |
| 空列表／全 null → `null` | U4 |

### 2. 三色灯三边界 + L1

证据：`WarningLightIntegrationTest`

| 边界 | 期望 | 结果 |
|---|---|---|
| 剩余 0 天（今天=预计完成日） | NONE（非蓝非黄） | 通过 |
| 预计完成时间为空 | 不参与蓝黄；停滞超阈值仍 RED | 通过 |
| 红黄同时满足 | RED | 通过 |
| L1：改需求名称 | `last_state_changed_at` 不变，红灯不消失 | 通过（走 `DemandService.update`） |
| 列表 `light=RED` | 只返回红灯需求 | 通过 |
| 8:30 快照 | 写入中文灯色，不抛异常 | 通过 |

### 3. 10 条 DERIVE_TASK + 课程开发截止日

证据：`TaskDeriveIntegrationTest.十条派生规则各触发一次`

| 任务类型 | 触发 | 备注 |
|---|---|---|
| 需求评审 | 登记需求 | — |
| 课程开发 | 课程立项 | **`due_date == expect_publish_date`** |
| 课程评审 | 提交评审 | — |
| 课程优化 | 评审不通过·修改后重新评审 | — |
| 讲师试讲 | 评审通过 | — |
| 签到导入 | 场次结束 | 与培训归档同一次转换 |
| 培训归档 | 场次结束 | — |
| 案例整理 | 课程标注达精品 → CREATE_CASE | — |
| 业务验收 | 标记交付使用 | — |
| 案例审核 | 提交审核 | — |

### 4. CLOSE_RELATED_TASKS

证据：`TaskDeriveIntegrationTest.终态关闭未完成任务`——立项后关闭课程开发，派生的「课程开发」任务状态变为「已关闭」。

### 5. 最小任务读接口（E3-5 后端一半）

证据：`TaskDeriveIntegrationTest.按负责人筛选与逾期` + `GET /api/tasks?ownerNo=`

- 按 `ownerNo` 筛选可用；无「我的任务」页签
- 逾期：`due_date < CURRENT_DATE` 且状态为待处理／处理中，SQL 实时算，不落库列

### 6. DEFERRED 与覆盖对账

证据：`EffectCoverageTest`——`DERIVE_TASK:`／`CLOSE_RELATED_TASKS` 已有处理器，且不在 `EffectDispatcher.DEFERRED`（该表已空）。

## 三、本段不做（留给后续段）

| 段 | 内容 |
|---|---|
| 3B | 数量类指标 + 驾驶舱 MetricCard 接线 + 列表灯色列展示 |
| 3C | 效率类指标 + 《指标口径核对表》骨架 |
| 3D | `GET /api/dashboard/overview` + V2 总看板／任务中心去 fixture |

## 四、硬约束核对

- 无缓存／无物化视图／无预聚合
- 快照只做变化检测（可发 `LightColorChangedEvent`），不发通知
- AR-3：聚合模块只读；任务写入在 app EffectHandler
- D-7／D-13 本段未改状态机
