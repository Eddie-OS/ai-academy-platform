# 阶段 3 · D 段（总看板 overview + 业务价值 + 预警明细 + V2 去 fixture）自检报告

> 对象：阶段 3D——`GET /api/dashboard/overview`、业务价值填报、预警汇总／明细、DashboardV2／TaskV2 产品模式接真 API。  
> 自检日期：2026-08-04。

## 一、本段交付

| 项 | 内容 |
|---|---|
| overview | `DashboardOverviewApplicationService`：CompletableFuture + CPU 核数线程池；装配 quantity／cockpits／warnings／worklist／efficiency／value／openTasks |
| API | `GET /api/dashboard/overview`；`GET /api/warnings`／`/summary`；`/api/value-reports` CRUD + `/summary` |
| 预警 | `WarningLightService.summarize`／`listDetails`（健康数不可下钻） |
| 前端 | DashboardV2／TaskV2：`?fixture=1` 仍 fixtures；产品模式接 overview／tasks；`/value-reports` 填报页 |
| 测试 | `DashboardOverviewIntegrationTest` |

## 二、出口核对

| # | 出口 | 证据 | 状态 |
|---|---|---|---|
| 1 | overview 单接口聚合 | `DashboardOverviewIntegrationTest` | 见跑数 |
| 2 | 业务价值年度汇总 | ValueReport + overview.value | 已接线 |
| 3 | 预警健康数 + 三色 | WarningSummary | 已接线 |
| 4 | V2 产品模式去 fixture | DashboardV2／TaskV2 + isRegressionMode | 已接线 |
| 5 | 无「我的任务」 | Task 页签「按负责人」+ ownerNo | 已对齐 AC3 |

## 三、本轮补齐

- 效率区近 6 个月趋势：`EfficiencyMetricsService.trendsLast6Months`（15.2.3 终点月归属）→ overview.`efficiencyTrends`；DashboardV2 产品模式折线接真序列
- 预警明细页：`/warnings` + 总看板三色卡「更多」下钻；健康数不可下钻
- E3 出口总表：[`阶段3-出口准则自检报告.md`](./阶段3-出口准则自检报告.md)

## 四、仍待人工

- E3-1 人工手算签字；E3-4 首屏 ≤3s 环境计时

## 五、纪律

- 无缓存／无预聚合
- overview 线程池限 CPU 核数
- 回归模式 fixtures 不动，视觉基线不受影响
