# 阶段 4A　催办主线自检报告

> 自检日期：2026-08-04。依据：阶段 4 提示词实现清单 1～5、7；开发 5.8。

## 交付物

| 项 | 路径／证据 |
|---|---|
| 迁移 | `V4_001__escalation_record_snapshots.sql`、`V4_002__cfg_escalation.sql` |
| 滚动周期 | `EscalationCycle` + 单测 `EscalationCycleTest` |
| 待催办清单 | `GET /api/escalations/pending` → `EscalationPendingApplicationService` |
| 标记已催办 | `POST /api/escalations`，D1 → `URGE_TOO_FREQUENT` + `force` |
| 台账列表 | `GET /api/escalations` |
| 催办配置 | `GET/PUT /api/config/escalation` + Settings「催办配置」Tab |
| 前端 | `MessageV2Page` 产品模式接 API；文案「标记已催办」／「待催办清单」 |

## 自动化

| 测试 | 结果 |
|---|---|
| `EscalationCycleTest` | 通过 |
| `EscalationIntegrationTest`（防重复+force、名称快照、清单分组） | 通过 |

## 复核修正（2026-08-04）

`EscalationPendingMapper` 初版把五个维度的状态过滤条件写死在 SQL 文本里，撞 E2-6 的
`StateLiteralGuardTest`。已改为从状态机取常量与 `terminalStates()` 后作为参数传入，
`CaseStateMachines`／`CourseStateMachines`／`DemandStateMachines`／`TrainingStateMachines`
补出对应的状态常量，并由该门禁断言常量与转换表一致。

## 硬约束核对

- 无消息发送路径、无空 `send()`、无 SENT 状态字段
- 清单为实时视图，非持久化消息表
- RM1 无每周一 `@Scheduled`，用周期边界纯函数
- 防重复查台账表，默认 24h（可配置），不用 Redis
