# 阶段 5　缺陷台账（E5-5）

> P0／P1 必须关闭才能上线；P2 写明后续版本。缺失功能只进二期，不在本阶段实现。
>
> 2026-08-04 结存：**P0 1 条、P1 9 条全部关闭**；P2 关闭 4 条、**开启 3 条**（均为待业务裁决的书面例外候选，不阻塞上线）。

## 开启中

| ID | 级别 | 描述 | 状态 | 计划 |
|---|---|---|---|---|
| D5-11 | P2 | 静止态主按钮白字对比度 **4.16:1**，未达 SC 1.4.3 的 4.5:1 | **待业务裁决**。事实已由 `contrast.test.ts` 钉住，走查表 A11Y-01a 已登记 | 与 V-4／V-5 同类处置：要么签书面例外，要么把静止态填色换成 hover 那一档（`#2F67ED`，4.90:1）——后者要重做九页视觉基线，不在阶段 5 范围 |
| D5-12 | P2 | 九页共 **94** 个可点目标小于 24×24（SC 2.5.8） | **待业务裁决**。逐页实测值已作为棘轮基线写入 `a11y.spec.ts`，只增会红 | 主因是 AntD `size="small"` 文字按钮（26×19）、表格勾选框（13×13）、日历场次条（105×12）。改动等于换组件密度，二期随设计迭代一并处理 |
| D5-13 | P2 | `/demands`（12 列）、`/trainings`（8 列）、`/cases`（4 列）的 `th` 缺 `scope` 属性 | 已确认，`/lecturers`／`/tasks`／`/reviews` 有 | 三页用的是裸 AntD Table，其余走 `DataTable`。二期统一到 `DataTable` 时自然消解；本阶段不动，避免改表格渲染影响视觉基线 |
| A11Y 人工项 | — | 灰度可区分、200% 缩放外的 8 条人工走查 + 浏览器兼容四格 | 待人工 | 见走查表结论 |

## 已关闭（阶段 5）

| ID | 级别 | 描述 | 根因 | 处置 | 证据 |
|---|---|---|---|---|---|
| S5-V9 | P0 | 前后端三色灯蓝黄语义颠倒 | 需求 13.4 与设计稿口径不一致，业务裁决 V-9 定「蓝=正常运行」 | `V5_001__calc_light_v9.sql` + `WarningLightService` + `LightFilterSql`；需求／CLAUDE／阶段 3 提示词同步 | `WarningLightIntegrationTest` 8 例 |
| D5-1 | P1 | 阶段 5 新增的三个 PowerShell 脚本一运行就报「字符串缺少终止符」 | 存成 UTF-8 **无 BOM**，Windows PowerShell 5.1 按 ANSI 读，中文字符串被截断成非法语法。项目原有脚本（`backup/build/deploy/smoke-test/seed`）都是 UTF-8 BOM，这三个漏了 | 三个脚本改存 UTF-8 BOM | `seed-perf.ps1`、`run-p1-p6.ps1`、`fetch-templates.ps1` 均可直接运行 |
| D5-2 | P1 | 性能造数在浏览记录那步整体回滚 | `seed-perf.sql` 写了 `dtl_case_view.viewer_no`，真实表里没有这一列（一期只有两个共享账号，粒度是 `account_type`，AC1） | 改用 `account_type='USER'` | `perf seed ok / courses 10000 / attendance 20000 / views 100000` |
| D5-3 | P1 | 性能脚本登录第一步就抛异常 | 用 `GET /api/meta/enums` 引导 XSRF Cookie，而该接口未登录返回 401，`Invoke-WebRequest` 直接抛 | 改用 `GET /api/auth/current`（未登录也回 200，与 `smoke-test.ps1` 同一套路） | `run-p1-p6.ps1` 四项全跑通 |
| D5-4 | P1 | P6 只测「发起成功」，等于没测 10000 行导出 | ① 计时到发起为止，>2000 行会转异步；② 轮询读 `state` 字段，实际字段叫 `status`，取值 `RUNNING`／`DONE`，读空后立刻下载得到 422 | P6 改为计时到**文件到手**，按 `status` 轮询到 `DONE` 再下载，并报出 `rowCount` | P6：异步 10001 行 2549.7ms |
| D5-5 | P1 | 前端状态硬编码门禁（STK-1／E2-6）红 | `ReviewV2Page.tsx` 写死了状态值「待录入结论」（KPI 标题 + 页签比较）与「待处理」（"N 条待处理"文案） | 页签取值在 `fixtures/review.ts` 导出为 `REVIEW_PENDING_TAB`，页面只引用常量；文案改「共 N 条」避开任务状态值子串 | `stateLiteralGuard.test.ts` 3 例通过 |
| D5-6 | P1 | 验收 A2-2 无对应实测 | 原有 `WriteApiPermissionTest` 只验「写接口都带 @WriteApi」+ 三个合成接口的行为，没有按 A2-2 要求**逐个调用**真实写接口 | 新增参数化断言：遍历容器里注册的全部写接口，用用户账号真发请求断言 403；白名单钉死为点赞／评论／停留时长回报三条，免登录档钉死为登录／登出两条 | `WriteApiPermissionTest#全部写接口对用户账号一律403` |
| D5-7 | P2 | P5（200MB 上传 + 看板干扰）原定真机人工勾选，无可回归的证据 | 计划把它列为人工项 | 脚本化为 `scripts/perf/run-p5.ps1`：分片上传放独立进程、主进程并发打看板，同时验断点续传与 F2 合并校验 | P5 PASS，见性能测试报告 |
| D5-8 | P1 | 九个页面的浏览器标签标题全是「AI学院联合作战平台」，分不出哪个标签是哪一页（SC 2.4.2 Level A） | SPA 只有 `index.html` 里那一个 `<title>`，路由切换后没人改它 | `shellNav.ts` 新增 `resolveDocumentTitle()`，取侧栏／顶栏同一份页名拼成「页名 · 平台名」，`AppShellV2` 按 pathname 设置。**不另起一套文案**——同一页三处名字必须一致 | `a11y.spec.ts` 九页逐个断言 |
| D5-9 | P1 | 没有「跳至主内容」，键盘使用者每翻一页要先按十几次 Tab 才够到内容（SC 2.4.1 Level A） | 侧栏 11 项在每页重复出现，缺少绕过机制 | 壳层加 `.shell-skip-link`，未聚焦时用 `clip` 移出可视区（不是 `display:none`，那样拿不到焦点）；主区加 `id` + `tabIndex=-1`，回车后焦点真的落进去 | `a11y.spec.ts`：Tab 第一站是它、聚焦后高 ≥24px、回车后 `activeElement` 是 `#shell-content`；131 张视觉基线零变化 |
| D5-10 | P1 | `/escalations` 有两个 `<main>` 地标（SC 1.3.1） | `MessageV2Page` 内层容器写成了 `<main className="esc-main">`，套在壳层的 `<main>` 里 | 换成 `div`。几何全靠 `.esc-main` 类，元素名不参与布局 | `a11y.spec.ts` 断言整页 `<main>` 恰好 1 个 |
| D5-14 | P2 | 走查表的对比度是**手算**填进去的，Token 改了不会有人重算 | 「约 1.2:1」这类结论写死在文档里，与代码无联动 | 新增 `contrast.test.ts` 实现 WCAG 相对亮度公式，7 例断言：该达标的达标，**已知不达标的断言它确实不达标**——例外一旦不再需要，测试会红着提醒去撤销它 | `contrast.test.ts` 7 例；`scripts/contrast-report.mjs` 可打印全表 |
| D5-16 | P2 | `frontend/playwright-report/`、`frontend/test-results/` 未进 `.gitignore` | 阶段 2 引入 Playwright 时只忽略了 `dist/`。这两个目录每跑一次视觉回归就重写，里面是 HTML 报告与失败 trace（几 MB 二进制） | 补进 `.gitignore` | `git status` 下两个目录不再出现在待提交清单里 |
| D5-15 | P2 | 安全检查报告有 7 处人工 ☐，其中「未登录直链」只测了后端 8080 | 反向代理是鉴权的旁路点：nginx 上多一个指向附件目录的 location，后端 401 就形同虚设，而后端侧测试全绿 | 补过 nginx 的 F3 探测（401 + 无目录列表）、生产档日志脱敏实测；「抽 5 个写接口」升级为遍历全部 | 见安全检查报告五节 |

## 二期清单（本阶段发现、明确不做）

| 项 | 来源 |
|---|---|
| 导入批次进行中／历史折叠 | 设计走查 |
| 前端 5000 行预检 | P-3 |
| 负责人配置 Tab | D-5 |
| OpenAPI 生成替换全部手写 interface | P-1（脚本已备） |
| 需求态势图换 ECharts | P-4 |

## P2 后续版本计划

| ID | 描述 | 目标版本 |
|---|---|---|
| D5-11 | 主按钮静止态填色改用 hover 那一档以满足 4.5:1 | 二期（随视觉基线一起重做） |
| D5-12 | 小号控件目标尺寸提到 24×24 | 二期（组件密度调整） |
| D5-13 | 三页表格统一到 `DataTable`，补 `th scope` | 二期 |
| A11Y-26 | 导入进度等异步状态补 `aria-live` | 二期 |

> P0／P1 全部关闭：本轮新增的 D5-8～D5-10 三条 P1 已在本阶段修完并有断言。
> 三条 P2 是待业务裁决的书面例外候选，不阻塞上线（E5-5 只要求 P0／P1 关闭）。
